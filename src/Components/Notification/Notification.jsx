import React from "react";

const Notification = (promp) => {
    return (
        <div className={promp.clsName}>
            <div className="result-form">
                this is for result form
            </div>
        </div>
    )
}

export default Notification;